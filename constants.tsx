import React from 'react';

export const DavinciResolveIcon: React.FC<{ className?: string }> = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>movie_edit</span>
);

export const SonyVegasIcon: React.FC<{ className?: string }> = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>movie_filter</span>
);

export const FinalCutProIcon: React.FC<{ className?: string }> = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>theaters</span>
);

export const HistoryIcon: React.FC<{ className?: string }> = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>history</span>
);

export const DownloadIcon: React.FC<{ className?: string }> = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>download</span>
);

export const ChevronDownIcon: React.FC<{ className?: string }> = ({ className }) => (
    <span className={`material-symbols-outlined ${className}`}>expand_more</span>
);